[package]
name = "isolate-endpoint"
version = "0.1.0"
edition = "2021"

[dependencies]
# Path relative to this Cargo.toml file, assuming both dirs are copied side-by-side
psor-sdk-lib = { path = "../psor-sdk-lib" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
